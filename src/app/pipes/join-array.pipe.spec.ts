import { JoinValidationErrorsPipePipe } from './join-array.pipe';

describe('JoinArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinValidationErrorsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
